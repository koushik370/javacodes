public class arthimatic_operators {
    public static void main(String[] args) {
        int a=7;
        a++;
        //a++; post increment
        //++a; pre increment
        int result=++a; //if we fetch the pre increment operator it will increment the value and retrun
        // int result=a++; if we fecth the post increment operator it will retrun and increment the value
        System.out.println(result);
    }
}
//operator like (+) , (-) , (+), (/) , (%)